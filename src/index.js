import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import { ElementalRouter } from './components/ElementalRouter';

const root = document.getElementById('root');

ReactDOM.render(
    <ElementalRouter />,
    root
);
